import { TokenInterceptor } from './token-interceptor';

describe('TokenInterceptor', (): void => {
	it('should create an instance', (): void => {
		expect(new TokenInterceptor()).toBeTruthy();
	});
});
