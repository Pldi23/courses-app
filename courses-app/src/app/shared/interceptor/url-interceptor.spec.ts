import { UrlInterceptor } from './url-interceptor';

describe('UrlInterceptor', (): void => {
  	it('should create an instance', (): void => {
  		expect(new UrlInterceptor()).toBeTruthy();
  	});
});
